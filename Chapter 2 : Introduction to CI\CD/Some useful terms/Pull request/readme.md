### Pull request
- In GitHub merging a branch back to the main branch of software is quite often happening using a mechanism called **pull request**(opens in a new tab), where the developer who has done some changes is requesting the changes to be merged to the main branch.
-  Once the pull request, or PR as it's often called, is made or opened, another developer checks that all is ok and merges the PR.
